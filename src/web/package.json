{
  "name": "dataset-web-ui",
  "private": true,
  "type": "module",
  "dependencies": {
    "preact": "^10.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.dev.mjs",
    "start": "python3 -m http.server 8080"
  }
}
