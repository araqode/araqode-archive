# araqode-archive
# A curated, multi-modal dataset

# Environment variable names
ENV_VARS = [
    "DATAVERSE_API_HOST",
    "DATAVERSE_API_TOKEN",
    "DATASET_DOI",
    "DATASET_NAME",
]

# Default dataset download directory inside workspace
DATASET_DOWNLOAD_DIR = "dataset"

# Temporary directory for caching dataset info
TMP_DIR = "tmp"
