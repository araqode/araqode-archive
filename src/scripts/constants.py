# Common constants for araqode-archive scripts
import os

# Environment variable names
ENV_VARS = [
    "DATAVERSE_BASE_URL",
    "DATAVERSE_API_TOKEN",
    "DATAVERSE_DATASET_PID",
    "REPO_NAME"
]

# Default local data directory
LOCAL_DATA_DIR = "dataset"
